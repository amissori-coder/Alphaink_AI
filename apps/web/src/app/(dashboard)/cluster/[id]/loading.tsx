import { ClusterEditorSkeleton } from '@/components/clusters/cluster-editor';

/** Scheletro mostrato durante la navigazione verso la scheda di un cluster. */
export default function ClusterDetailLoading() {
  return <ClusterEditorSkeleton />;
}
