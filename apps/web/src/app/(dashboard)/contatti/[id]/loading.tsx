import { ContactDetailSkeleton } from '@/components/contacts/contact-detail';

/** Scheletro mostrato durante la navigazione verso la scheda di un contatto. */
export default function ContattoDetailLoading() {
  return <ContactDetailSkeleton />;
}
